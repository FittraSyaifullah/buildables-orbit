# Server (Render/Railway/Fly inject PORT automatically)
PORT=3001
NODE_ENV=development

# Mapbox — required for globe map + geocoding
MAPBOX_ACCESS_TOKEN=

# AI synopsis — optional
EXA_API_KEY=
MISTRAL_API_KEY=
